# S10a 管理台与消费端验证

PASS：后端144项真实MySQL/HTTP/领域与架构测试，0失败/错误/跳过；前端TypeScript严格检查与生产构建通过；npm audit记录0项已知漏洞。浏览器4个完整场景均通过，数据由scripts/prepare-e2e.py写入独立租户，没有请求拦截或页面硬编码业务数组。

- 会员领券、真实报价104.00、提交地址与下单；管理员沙箱收款核对，真实异步订单与权益推进；发货签收、退货入库、退款核对及权益REVOKED。
- 可视规则编辑发布、低代码真实预览/审批/发布，以及390px消费端无水平溢出。
- 无效凭据拒绝，退出清除会话；凭据不进入浏览器持久存储。
- 可视旅程编排、审批发布、手工入组及持久站内信触达。

新增运营读取接口仍走领域API和租户过滤；/v1/admin/**统一角色门禁，应用服务继续二次校验。控制器不读取跨领域Mapper。截图已逐张查看：商品页、低代码预览、移动页布局可用。初轮暴露的表单标签关联、中文定位和加载图标离场后无障碍名称问题已修复；主主题关闭离场动画，未放宽业务断言。

当前单包约1.12MB/minified、356KB/gzip，是构建实测，不是性能SLA；Vite大包提示记录为后续按访问量拆分的优化项。初始本地管理员/会员令牌输入是明确隔离入口，真实IdP与渠道按用户要求后置。S10b还需jar静态资源打包、完整演示seed、容器、CI及整体审查；S10父项尚未DONE。
