# S8b1 券钱包和订单占用验证

PASS：118项，0失败/错误/跳过；真实MySQL与HTTP。7个新增用例覆盖配额并发、同券订单竞争、库存失败原子回滚、互斥择优/叠加、未知支付保留、全退返券/部分不返、旧事件不释放新占用、零元券单。

现有S4演示seed在新应用重启后重复执行成功：仍返回原报价84e6984b-5c1a-4e92-915e-cb99d258f2db、168.00。可选新请求字段采用NON_NULL序列化，保留原幂等摘要；新增字段不会令旧seed同键请求冲突。旧报价缺少coupon/sources字段仍可读取。

锁顺序审查：占券只锁钱包行，定义不可变普通读，避免领取锁定义后再锁券时形成反向锁序。最终库存、券和报价消费为同一数据库事务。消费者Inbox与返券同事务。

S8b2预算/权益发放/资金分摊仍未完成；S5b与S7b父项保留至该部分验收。未执行真实外部发券或权益联调。
