# 首批领域内核验证结果

日期：2026-09-23。范围 S1–S3；总体平台仍在建设。执行命令 `mvn -o -B verify`，真实退出 0。日志 [maven-verify.log](maven-verify.log)，类/构建文件指纹与 XML 测试汇总见 [verification.json](verification.json)。

**72 项通过，失败 0、错误 0、跳过 0。** 其中 Money/标识 3、规则 13、营销报价 10、订单生命周期 45、架构 1。随机分摊的 500 组样本包含在一个测试中，不额外计为 500 项测试。

| 验收 | 结果与证据 |
|---|---|
| 精确金额、上限与恶意指数 | PASS，MoneyTest；无浮点运算和静默舍入 |
| 三值逻辑及未知不能取反放行 | PASS，RuleEvaluatorTest 九组完整二元真值表及缺失/类型不符 |
| 资源边界和全树检查 | PASS，规则深度/节点、事实数量、文本/数字、请求行数/候选数超限 |
| 优惠版本、有效期、门槛和上限 | PASS，MarketingDecisionTest |
| 跨作用域、重复输入拒绝 | PASS，过期候选也检查 tenant/merchant/store 作用域 |
| 稳定择优及分摊守恒 | PASS，平局、输入换序、最大余数、long乘积超界场景、500组确定种子样本 |
| 不可变快照 | PASS，修改原集合不影响结果，输出集合拒绝写入 |
| 订单状态迁移 | PASS，全部7状态×6事件；支付中取消等待、迟到支付事实、终态/非法迁移及版本溢出 |
| 模块依赖与纯领域边界 | PASS，JDK jdeps 检查真实生产 class，api 不依赖实现，无营销/订单互引或框架依赖 |
| 参考源保护 | PASS，扫描记录的11个源码/配置样本 hash 无变化；这是抽样指纹，不外推全仓字节审计 |

独立验证按契约逐项对照实际 JUnit/Surefire XML 与产物摘要完成；本次没有启动独立子 Agent，不能宣称独立团队审计。全平台架构就绪、数据库事务/乐观锁/并发、HTTP鉴权、真实支付/权益、Outbox重启恢复、UI、性能与灾备均 **UNVERIFIED / 尚未实现**。状态机领域测试不证明数据库并发一致性。

审查结论：首批 C1–C4 范围内未发现阻断问题。后续代码发生变化后须重跑相关测试，不能沿用本报告的旧摘要作为新版本验收。
