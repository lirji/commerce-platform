# OP05 验证记录

- `./scripts/verify.sh` PASS，158测试，V20真实MySQL迁移成功。
- SegmentTest 4场景验证超过单批100人的检查点、未完成快照不可见、完整发布、创建截止时间、冻结会员排除、旧版本不可变、同人群任务合并、容量失败与取消、周期启动、调度版本冲突、保留命名空间、标签事实实际影响报价。
- 故障注入仅限测试租户：下一批投影冲突连续5次后ISOLATED，检查点不前移；修复冲突后从原检查点重试完整发布，无重复会员。
- RuleEvaluator新增精确标签集合比较测试，缺失事实在NOT下仍UNKNOWN。原数字全操作符测试更新为明确数字操作符集合，新增CONTAINS不允许数字类型。
- `npm run build` PASS；动态人群页面含可视规则、周期启停、任务进度/取消/重试和版本引用信息。
- 浏览器测试待OP08。刷新是区间视图，不宣称跨批次同一数据库快照。
