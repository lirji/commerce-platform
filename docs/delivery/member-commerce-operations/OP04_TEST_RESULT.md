# OP04 验证记录

- `./scripts/verify.sh` PASS，153测试。V19真实MySQL迁移成功，成长来源、账本、账户、标签持久化。
- MemberGrowthTest 3场景：退款先到/完成后重算、重复事实、新策略不改历史成长率、无策略历史订单不奖励、人工调整后退款债务、并发重复退款、标签版本/撤销、越权、真实HTTP订单→支付→签收→退款→Outbox成长闭环。
- `npm run build` PASS；UI提供等级策略发布、成长/账本、人工调整/重算、标签关联及会员本人只读成长。
- 首轮回归2项零元售后失败：成长消费者误校验正额退款导致共用消费事务回滚。已在装配边界识别零额无需资金冲回，原70项交易回归全部恢复通过；未弱化原测试。
- 浏览器交互在OP08验证；增长吞吐、生产经济策略不做未测承诺。
