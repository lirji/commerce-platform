# OP03 验证记录

- `./scripts/verify.sh` PASS，150测试；真实MySQL V18迁移成功。两个新增场景验证规格顺序规范化及重复约束、草稿不可销售、上下架、调价重放、过期版本、报价快照不变、撤权后的读写拒绝、资源换绑拒绝、非法规格、旧SKU继续可修改并形成历史。
- `npm run build` PASS；`git diff --check` PASS。
- 浏览器实际操作与重启后持久性观察将在OP08执行；当前仅API/持久化与UI构建已验证。
- 新SKU规格不可换绑，库存接口仍按SKU工作；原有兼容创建接口保留并开始写修订记录。
