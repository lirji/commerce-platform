# LP08 生命周期与效果比较证据

生日、沉睡、复购及加购未购买触发复用旅程审批与节点引擎；V32持久扫描单会员事务保存入组/游标，每租户每轮20步，失败五次隔离可恢复。会员状态和journeyEnabled当前锁读覆盖新入组与等待后执行；加购保存原锚点，再核对权威订单状态。COUPON节点使用独立JOURNEY来源及实例/节点去重。生日/UTC、频控和观察窗可配置。

旅程版本比较以会员首次入组为观察锚点，同版本会员/订单去重；批次比较通过实际couponId关联，只计券自身优惠。成功退款含窗口外退款；完整观察人数、事件/重建覆盖和成本范围明确，不显示伪造ROI。成长退款写路径改当前锁读，旧RR快照下不同退款不相互覆盖。

真实隔离MySQL V32。完整 `mvn -B -Pwith-ui verify` PASS，208项后端测试；最终 LifecycleJourneyTest 6项再次PASS，增加8天后退款仍冲减原队列及观察成熟断言。覆盖闰年/非闰年生日、周期重复、关闭偏好、手工拒绝、真实付款后停止加购等待、沉睡/净退款复购、并发扫描、配置损坏五次隔离和修复后恢复、实际用券付款/退款/幂等重建。原旅程、券、效果、成长专项15项PASS含新增RR并发测试。

前端最终构建及clean package PASS，8604运行独立副本 `.local/member-suite-lp08-final.jar`。8条会员真实浏览器场景全部PASS；生日模板→券节点→审批发布→持久扫描→会员到账→版本比较闭环PASS。截图人工检查，数据表采用横向滚动保留信息密度；侧栏和首页仍由LP11统一整理。种子重放PASS。

已解决：扫描TIMESTAMP毫秒精度与Instant纳秒边界导致尚未到期；模板默认入组时间向前取整过早导致新券发行窗口校验失败。验收中AntD虚拟选项和重复表头定位修正属于测试定位修正。

限制：加购以加购后创建且已付的同店订单排除，支付事实仍需推进订单权威状态；不关联加购前创建的旧未付订单。无真实渠道触达，仍为站内通知。混合旧执行器不可执行新节点，启用新定义前须全部升级；见LP08_CONTRACT发布兼容。原8602未更新。LP08完成，继续商品LP09/LP10及LP11整体验收。
