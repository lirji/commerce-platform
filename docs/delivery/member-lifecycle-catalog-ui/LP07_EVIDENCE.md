# LP07 定向发券与相对有效期证据

V31已迁移至隔离MySQL。批次固定券版本和人群快照，逐会员事务保存发券、频控、回执及游标；每轮最多20人。支持停止、失败退避/五次隔离、人工重试、撤销未使用券，持有/已使用券保留原因。券相对有效期从真实发放时刻计算，退款/取消不刷新期限，撤销不退发行配额。

完整 `mvn -B -Pwith-ui verify` PASS：201项后端测试。CouponDeliveryTest 6项及PointOfferTest 6项专项PASS，覆盖固定快照、并发泵送、跨批次频控、配额失败检查点/隔离/恢复、停止/过期、持有券撤销保护及相对有效期。未进行进程中途强杀演练，不将持久检查点验证等同完整故障演练。

前端最终构建、clean package PASS。实际8604运行独立副本 `.local/member-suite-lp07-final.jar`，workers=false。7条真实浏览器场景PASS：周期/权益2、积分1、兑换1、积分结算1、行为1、定向发券1。定向场景验证两位会员发放、本人钱包7日准确期限、撤销回执；会员导航不可见管理入口。截图人工检查PASS，种子脚本重放PASS。

构建注意：with-ui只复制已有frontend/dist；必须先npm run build，再Maven打包。所有最终页面验收使用重新构建后的运行包。原8602 Docker及用户原工作树未动。LP07完成，继续LP08。
