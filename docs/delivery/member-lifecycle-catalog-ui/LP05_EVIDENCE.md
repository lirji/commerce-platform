# LP05 积分兑换实施与独立验证

兑换目录支持券与权益、积分价格、总额度、每人次数、有效期及版本化停启。扣分/限额/来源发放/回执同事务；权益受理后仍由既有Outbox消费者发放。SOURCE_ONLY券无法通过公开领取绕过价格，免费领取历史约束保留，兑换来源券可多次获取。V29已在隔离MySQL应用。

`mvn -B -Pwith-ui verify` PASS：189项测试。最终删除无用测试辅助方法后PointOfferTest 6项再验证PASS，覆盖券和权益、幂等、资产不足全部回滚、会员及跨会员总限额并发、停启版本冲突、到期/冻结/越权、消费奖励退款产生待偿积分。未通过测试不掩盖：初次免费领取测试用了不存在路由返回404，修正为真实路由后验证409。

前端类型/构建PASS。会员周期/积分/兑换/结算共5个Playwright场景PASS，兑换场景包含创建目录、真实扣分、回执、券与权益到账、停启后目录变化。种子重放PASS，不重复赠分发券。截图人工检查PASS。

接口和边界见LP05_CONTRACT.md。成功兑换无主动退积分入口，券/权益过期不回兑；本片不含定向批次与相对券有效期（LP07）。没有新增框架/组件。LP05 DONE，继续LP06行为与人群。远程CI、主线Git及新版Docker尚未执行。
