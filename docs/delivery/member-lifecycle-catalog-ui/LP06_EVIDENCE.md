# LP06 会员行为、人群与详情证据

新增生日月日/站内旅程偏好，服务端时间的商品浏览/加购信号及按会员日限200，近30个UTC自然日统计，完成订单与退款净现金投影，管理员/本人详情与行为明细，分页补建历史成交。动态规则新增8个事实字段，无成交间隔保持UNKNOWN。一次分群批量查询，不做逐会员网络N+1。

V30已实际迁移MySQL。完整 `mvn -B -Pwith-ui verify` PASS：195项后端测试，包含MemberBehaviorTest 6项，真实订单支付/履约事件与补建、重复/退款、跨主体、日额度与跨日窗口、闰日生日、偏好CAS、人群命中。后续将目录校验放进命令幂等边界，增加“商品下架后同键回放”断言，重新编译及6项专项PASS。首次圈选测试时固定时钟早于数据库会员创建时间导致0人，修正测试时钟后PASS，没有改业务筛选截止时间。

前端构建PASS。真实浏览器1个完整场景PASS：查看商品/加购入库→本人修改生日/关闭偏好→管理员会员详情→按加购规则生成动态人群。截图人工检查PASS；种子重放PASS。后端最终改动是保持同键回放语义，页面契约没有变化，后续整体验收继续覆盖。

限制：LP08将使用旅程偏好，当前未宣称旧旅程已受该偏好控制；新详情展示偏好本身。浏览加购是交互信号，不直接发放资金。行为原始保留期限尚未确认，不自动删历史数据；没有新中间件或外部触达。LP06 DONE，继续LP07定向发券及相对有效期。
