# LP10 商品计划与渠道价验收

V34追加可信凭据/订单渠道、当前渠道价及不可变历史、批量经营任务与逐项回执。任务固定SKU预期修订，每项事务同时提交商品修订/审计/回执/游标。定时、截止、冲突跳过、取消、退避5次隔离与恢复均持久化；每租户每轮20项、轮转4租户。执行重验原OPERATOR身份及当前门店CATALOG授权，ADMIN创建视为显式后台委托。失败后记录前重验版本，避免将另一执行器已完成的失败归到下一项。

可信销售渠道来自platform_credential，默认WEB，不接受用户自报。目录、详情、筛选和报价统一渠道选价；报价固化渠道/价格版本/窗口期限，订单记录渠道。改价不改有效旧报价，跨渠道消费拒绝。经营目录仍显示基础价。

验证：完整MySQL后端218项PASS（含架构边界），商品专项12项PASS。5个CatalogSchedulingTest覆盖并发CAS/重复推进、定时/截止、固定版本冲突、撤权隔离恢复、身份失效、跨渠道hash/下单拦截、未知channel请求400、期限/停用回退、价格历史及旧JSON缺省兼容。MemberCycleTest调整为有界等待轮转处理当前夹具，避免历史测试租户积累时错误假设一轮全局20人必定包含本租户；业务预算未扩大。

前端构建PASS；商品资料及新经营计划浏览器2条PASS，含实际调价27.00、持久回执、MINI_APP价19.00与历史。两页截图人工复核无遮挡/白屏，pageerror为空。批次ID在同一意图重试时稳定，避免未知结果重试生成新任务。

8604运行.local/member-suite-lp10.jar，隔离库V34，workers=false，手动推进按相同预算。原8602未更新。启用新渠道/任务前要求所有目录/报价/订单/worker写入实例升级；取消不回滚已执行项目，旧版本不理解新渠道快照时不可直接回滚应用。无新增依赖或中间件。
