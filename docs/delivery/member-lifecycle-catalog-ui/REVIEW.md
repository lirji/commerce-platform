# 实现审查（同会话源码与运行证据）

未启动独立Agent；以下为本次逐片审查与真实测试，不伪称第三方审计。

- 经济不变量：积分批次/冻结/原订单行退款、已消费奖励债务、兑换失败回滚、资金DECIMAL比较及成长并发退款。对应LP03–LP05、LP08专项及真实MySQL测试；未用Mock证明事务。
- 授权：租户从Actor取得，经营写与执行都重验门店权限；生命周期每个执行节点检查当前会员偏好；MINI_APP渠道来自凭据，公开请求未知channel拒绝，跨渠道报价消费拒绝。CatalogSchedulingTest及权限专项覆盖。
- 长任务：固定输入/人群，逐项效果与回执检查点同事务，失败退避5次隔离、恢复保持游标；经营失败记账前复核版本，防并发旧执行器污染下一项目。取消不宣称业务回滚。
- 商品：绑定模板与创建SKU使用相同SPU锁序；条码DB唯一且先验证ASCII再大写；价格/模板/图片等不同修订不混写；报价保存成交快照。V33/V34为追加迁移，旧脚本未改。
- 分析：会员/目录各域提供Stats，insight按订单唯一投影按UTC日聚合，app无跨域SQL；BigDecimal汇总展示金额。54名会员、跨门店、UTC窗口端点及退款更新测试防止首页计数和浮点求和。
- UI：同一深色token体系，真实API与库内种子；按页lazy加载，错误不展示虚构0；折叠搜索导航与390px布局；桌面/手机截图及页面错误监听。既有全链路测试适配新首页后重跑。
- 数据与交付：没有改原工作树用户文件，没有清数据、引入中间件、变更密钥或提交令牌。所有新增SQL表/列有中文注释。种子通过业务API产生可核对订单和退款。

边界：资料图片采用URL，无文件上传服务；渠道为受控本地凭据，不等于已完成微信/支付渠道联调；关联效果不代表因果提升/利润。数据库UTC连接参数是现有运行约定。升级后旧镜像无法解析全部新快照，必须使用兼容修复和业务补偿，不能盲目回退。全局预算下单轮worker不保证遍历所有历史租户，运行方需观察积压。

最终门禁以LP11_TEST_RESULT、CI_RESULT与DEPLOYMENT_RESULT分别为准，源代码完成不等于容器已更新。
