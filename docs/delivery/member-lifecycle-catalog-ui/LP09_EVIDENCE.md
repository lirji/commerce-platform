# LP09 商品资料与检索证据

V33真实迁移MySQL：三级类目、固定版本规格模板、商品说明/图片/模板类目绑定、门店唯一条码。SPU锁串行化首次模板绑定和SKU创建；规范化组合仍唯一；模板已绑定不可换版，旧自由规格不强绑。条码独立版本与门店唯一约束，不影响销售报价修订。经营与会员检索按门店、上下架、文字/条码、类目、价格区间及稳定SKU游标读取。

完整 `mvn -B -Pwith-ui verify` PASS，213项后端测试。CatalogMerchandisingTest 5项及ProductOperationsTest 2项最终专项PASS，覆盖类目停用/深度/撤权、固定模板值与图文URL边界、模板绑定对撞创建规格、大小写条码并发唯一、版本冲突、销售筛选、租户门店隔离、旧接口与历史报价兼容。最后收紧ASCII原值再转大写，防止Unicode折叠绕过；专项7项复验及8604真实HTTP 400验证PASS。

最终前端构建/打包PASS；9条商品+会员浏览器链路PASS，原门店运营单独1条PASS含即时撤权。新闭环：新类目/模板→新SPU绑定→下拉选择模板规格→条码→上架→会员按条码筛选→真实图片与商品说明。图片检查naturalWidth及容器边界，页面无运行错误。种子重放PASS，图片URL与商品数据均从数据库接口读取。公开包内媒体HTTP200，匿名业务目录HTTP401。

页面验收修复了弹窗与筛选字段重复DOM ID、关闭详情时旧读取数据引用已清除选中项导致白屏、图片grid最小高度造成价格遮挡。截图最终人工复核PASS。最新运行副本.local/member-suite-lp09-verified.jar，隔离库V33，workers=false；原8602仍未替换。

范围：图片为URL管理与静态演示插图，没有新增文件上传/对象存储；条码是经营标识，不宣称GS1认证；关系库筛选不承诺大规模全文检索性能。绑定模板前所有商品写入实例应升级，旧写入器不识别模板约束。LP09完成，继续LP10批量/定时经营与可信渠道价格。
