# 本地Docker部署记录

状态：PENDING，等待本轮最终CI通过后更新本地应用。

目标：现有commerce-platform/app，127.0.0.1:8602，同源UI/API；继续复用dev-infra MySQL，commerce_local。现有版本fa97292，数据库V22。

范围：用户已授权本地Docker前后端部署，不包含生产环境。应用镜像以本次验证commit与jar摘要标识，保留原密钥、数据库及共享组件。

回退：V23–V34为追加迁移；新能力启用后旧代码不能解析全部快照。部署失败先停新入口与worker、保留数据，修复到可读取新格式的兼容版本，不能盲目回滚旧镜像或清库。实际版本、健康、业务烟测与镜像摘要待执行后填入。
