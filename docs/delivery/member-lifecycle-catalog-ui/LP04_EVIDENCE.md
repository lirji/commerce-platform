# LP04 实施与独立验证证据

积分报价/冻结/核销/取消/部分退款已接通。积分抵扣由服务端配置与原批次权威余额计算；UNKNOWN保持冻结；零现金订单仍有积分返还。部分退货使用累计分摊保证整数积分与现金分别守恒。新前端购物袋、订单详情和售后展示真实积分。

验证：最终 `mvn -B -Pwith-ui verify` PASS，183项后端测试；PointsCheckoutTest 8项与MemberPointsTest 5项覆盖并发占用、旧RR快照、取消未知、全积分零现金、三次部分退款、原期限失效、欠项抵偿、旧快照读取。V27/V28实际应用MySQL 8.4隔离库。前端类型与构建PASS；周期/积分3个浏览器场景PASS，新增积分结算场景PASS，截图人工检查PASS。

发现并修复：MySQL字符串金额参与加法隐式转浮点，43.32+21.67对64.99误判超限；退款预留显式DECIMAL转换并由三次部分退款回归覆盖。旧JSON缺新增long字段在Jackson严格模式报错；可空入口归一为0并测试旧报价/售后。持有分配与返还去重使用当前锁读，避免等待会员锁后仍使用旧RR快照。

范围：member/trade/order-runtime/aftersales/payment必要金额修正、app迁移/集成测试、对应前端、种子与本片文档。无依赖升级。首次浏览器用例错误假定取消需要确认弹窗，修正为既有单击命令后PASS，没有放宽业务断言。

限制：兑换券/权益为LP05，未计入本片；未宣称性能、远程CI或Docker新版部署通过。启用积分写入后不支持回退到旧积分无感知应用，见契约。原8602 Docker保持原版本。LP04 DONE，继续LP05。
