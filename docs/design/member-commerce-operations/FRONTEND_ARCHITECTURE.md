# 前端方案

复用 React/Ant Design 壳层、Bearer 会话、useResource/useCommand，新增会员经营、商品编辑、权限、成长、动态人群和效果面板。保持原有创建表单可用。配置使用表单/受限规则编辑器，显示版本、状态、发布/刷新时间。会员端展示真实等级与成长，禁止硬编码演示数据。

每个写操作显示等待状态、失败消息和幂等重试；409 说明数据已更新并提供刷新；403 不呈现为无数据；未配置、空集和载入错误分别展示。店铺切换清空旧资源。OPERATOR 导航仅显示受授权功能，服务端仍做实际校验。关键编辑用明确保存/发布，不用页面切换自动提交。
