<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lrj.commerce.runtime.persistence.CredentialMapper">
 <select id="activeOperator" resultType="boolean">SELECT EXISTS(SELECT 1 FROM platform_credential WHERE tenant_id=#{tenant} AND actor_id=#{actor} AND role='OPERATOR' AND active=TRUE AND expires_at&gt;UTC_TIMESTAMP(3))</select>
 <select id="findCredential" resultType="com.lrj.commerce.runtime.persistence.CredentialMapper$Credential">SELECT tenant_id,actor_id,role,sales_channel channel FROM platform_credential WHERE token_hash=#{tokenHash} AND active=TRUE AND expires_at&gt;UTC_TIMESTAMP(3)</select>
</mapper>
