<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lrj.commerce.runtime.persistence.CredentialMapper">
 <select id="authenticate" resultType="com.lrj.commerce.runtime.api.Actor">SELECT tenant_id,actor_id,role FROM platform_credential WHERE token_hash=#{tokenHash} AND active=TRUE AND expires_at&gt;UTC_TIMESTAMP(3)</select>
</mapper>
