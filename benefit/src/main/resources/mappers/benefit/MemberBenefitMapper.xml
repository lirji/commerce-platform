<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lrj.commerce.benefit.infrastructure.MemberBenefitMapper">
 <insert id="insert">INSERT INTO benefit_level_bundle(tenant_id,binding_id,policy_version,member_level,store_id,valid_until,bundle_json) VALUES(#{tenant},#{b.bindingId},#{b.policyVersion},#{b.level},#{b.storeId},#{b.validUntil},#{json})</insert>
 <select id="find" resultType="string">SELECT CAST(bundle_json AS CHAR) FROM benefit_level_bundle WHERE tenant_id=#{tenant} AND policy_version=#{policy} AND member_level=#{level}</select>
 <select id="list" resultType="string">SELECT CAST(bundle_json AS CHAR) FROM benefit_level_bundle WHERE tenant_id=#{tenant} AND policy_version=#{policy} ORDER BY member_level LIMIT 8</select>
</mapper>
