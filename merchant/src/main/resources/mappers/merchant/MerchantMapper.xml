<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lrj.commerce.merchant.infrastructure.MerchantMapper">
 <insert id="insert">INSERT INTO merchant_record(tenant_id,merchant_id,name,status,version) VALUES(#{tenant},#{input.merchantId},#{input.name},'ACTIVE',0)</insert>
 <select id="find" resultType="com.lrj.commerce.merchant.api.MerchantApi$View">SELECT merchant_id,name,status,version FROM merchant_record WHERE tenant_id=#{tenant} AND merchant_id=#{id}</select>
 <select id="list" resultType="com.lrj.commerce.merchant.api.MerchantApi$View">SELECT merchant_id,name,status,version FROM merchant_record WHERE tenant_id=#{tenant} AND merchant_id &gt; #{after} ORDER BY merchant_id LIMIT #{limit}</select>
</mapper>
