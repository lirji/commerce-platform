<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lrj.commerce.ordering.infrastructure.OrderMapper">
 <sql id="header">order_id,member_id,store_id,merchant_id,quote_id,CAST(payable AS CHAR) payable,status,payment_kind,version,created_at,expires_at</sql>
 <insert id="insert">INSERT INTO order_record(tenant_id,order_id,member_id,store_id,merchant_id,quote_id,payable,status,payment_kind,version,created_at,expires_at,items_json,address_cipher,address_key_version) VALUES(#{tenant},#{view.orderId},#{view.memberId},#{view.storeId},#{view.merchantId},#{view.quoteId},#{view.payable},#{view.status},#{view.paymentKind},#{view.version},#{view.createdAt},#{view.expiresAt},#{items},#{address},1)</insert>
 <select id="read" resultType="com.lrj.commerce.ordering.infrastructure.OrderMapper$Row">SELECT <include refid="header"/>,items_json FROM order_record WHERE tenant_id=#{tenant} AND member_id=#{member} AND order_id=#{id}</select>
 <select id="lock" resultType="com.lrj.commerce.ordering.infrastructure.OrderMapper$Row">SELECT <include refid="header"/>,items_json FROM order_record WHERE tenant_id=#{tenant} AND member_id=#{member} AND order_id=#{id} FOR UPDATE</select>
 <select id="list" resultType="com.lrj.commerce.ordering.infrastructure.OrderMapper$Row">SELECT <include refid="header"/>,NULL AS items_json FROM order_record WHERE tenant_id=#{tenant} <if test="member != null">AND member_id=#{member}</if> AND order_id&gt;#{after} ORDER BY order_id LIMIT #{limit}</select>
 <update id="change">UPDATE order_record SET status=#{status},version=version+1 WHERE tenant_id=#{tenant} AND order_id=#{id} AND version=#{version}</update>
 <select id="hasPaidSince" resultType="boolean">SELECT EXISTS(SELECT 1 FROM order_record WHERE tenant_id=#{tenant} AND member_id=#{member} AND store_id=#{store} AND created_at&gt;=#{since} AND status IN ('PAID','FULFILLING','COMPLETED'))</select>
 <select id="internalRead" resultType="com.lrj.commerce.ordering.infrastructure.OrderMapper$Row">SELECT <include refid="header"/>,items_json FROM order_record WHERE tenant_id=#{tenant} AND order_id=#{id}</select>
 <select id="internalLock" resultType="com.lrj.commerce.ordering.infrastructure.OrderMapper$Row">SELECT <include refid="header"/>,items_json FROM order_record WHERE tenant_id=#{tenant} AND order_id=#{id} FOR UPDATE</select>
 <select id="expired" resultType="com.lrj.commerce.ordering.infrastructure.OrderMapper$Row">SELECT <include refid="header"/>,items_json FROM order_record WHERE tenant_id=#{tenant} AND status IN ('PENDING_PAYMENT','PAYMENT_IN_PROGRESS') AND expires_at&lt;=#{now} ORDER BY expires_at,order_id LIMIT 20 FOR UPDATE SKIP LOCKED</select>
</mapper>
